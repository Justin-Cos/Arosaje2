import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'version_mobile',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
