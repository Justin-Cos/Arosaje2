FROM node:20

WORKDIR /usr/src/app

COPY package*.json ./

RUN npm install

COPY . .

ARG JWT_SECRET
ARG REFRESH_SECRET
ARG SALT

ENV JWT_SECRET=$JWT_SECRET
ENV REFRESH_SECRET=$REFRESH_SECRET
ENV SALT=$SALT

ENV NODE_ENV=container

EXPOSE 3000

CMD ["npm", "start"]